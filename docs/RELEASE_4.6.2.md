# v4.6.2 发布准备

状态核对：2026-09-24。本版本本地实现与最终验证已完成，尚未合入 main、打标签或部署。按用户最新安排，推送 `feat/banner-stats-and-guide` 并创建发布 PR、检查 CI 后暂停，等待并行的新卡池适配任务完成及用户后续合并指令；不设置自动合并，不调整自动部署配置。线上仍为 `v4.6.0`；生产统计迁移、常驻 Worker 与 `public-statistics-v4` 快照预热尚未执行，须先完成这些步骤再启用对应前端与 API。

## 交付范围

- 单池与五类合池：限定角色、限定武器、常驻武器、重构寻访、重构申领。常驻角色、启程与辉光庆典不进入合池。
- 首次样本先逐账号、逐期、逐对象计算，再按对象当期身份汇总为目标、其他限定、常驻与待分类六星；首次不跨期，类别不作为虚拟物品。账号覆盖按完整账号身份跨期去重，样本数与账号数分别解释。
- 旧 `metrics` / `resources` 按选择的单池或合池迁入快照。保底间隔与资源拷贝顺序仍使用完整相关账号上下文；`resultsPerTarget` 不解释为首获均值，未知身份与未知成本不填零。
- 统计工作区提供十图（原八图加账号覆盖、花费区间）、目标速览、头像对象选择、分类及拼音排序、分位数读数、频数分组、深色主题与减少动态效果。
- 全服和个人统计只读持久结果，5/30/60 分钟队列按受影响范围重算；成员变化、修订冲突与读取不完整不会发布不完整结果。首次未就绪显示等待，已有结果保留真实计算时间。
- 各桌面页面、顶栏与底栏统一沿用首页响应式宽度，覆盖经典入口及长内容页。首页无需纵向下滑的合同不扩展到长统计或设置页。
- 首次使用指南仅在 `/statistics-preview.html` 开发预览中提供状态样例，未接入真实登录、导入、备份或完成状态，不作为已上线功能宣传。

统计口径见 [STATS_OBSERVATION_CONTRACT.md](STATS_OBSERVATION_CONTRACT.md)，运行方式见 [STATISTICS_SCHEDULING.md](STATISTICS_SCHEDULING.md)，布局合同见 [DESKTOP_HOME_DEMO.md](DESKTOP_HOME_DEMO.md)。v4.6.0 及更早的发布记录继续保留其历史含义；独立后端与第三方依赖沿用各自版本。

## 已有验证

以下是本版本已完成的最终本地验证，仍不替代合入 main 后的 CI 与生产证据：

- 算法优化后全量单元测试 269 个文件、1,498 项通过，改动 ESLint 与主应用构建通过；优化前完整 ESLint、主应用与抽奖子应用构建及 1,495 项测试通过。
- PGlite 统计 SQL 行为验证和覆盖 186 个迁移的 baseline 验证通过；本地数据库验证不等同于生产迁移已执行。
- 生产依赖安全审计：内部镜像不支持 `npm audit`，改用官方 registry 执行 `npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org`，结果为 0 漏洞。
- 真实浏览器已检查桌面 1366～3840 宽度、合池 360～1920 宽度，以及对象选择、排序、图表、亮暗主题、减少动态效果和移动统计入口。
- 最大限定角色合池：1,009,281 个有效结果、2,675 个去重账号，独立任务约 70 秒，匿名响应约 96.5 KB。批量本地预览涵盖 34 单池、5 合池及 3 份旧缓存；这些观测不是生产性能承诺。
- 优化后用同一批 1,985,909 条真实记录核对新旧算法，34 单池＋5 合池全部字段一致。独立任务最大限定合池峰值 733MiB，连续五组合池进程峰值 810MiB；24 万条合成记录／72 范围的观测和旧指标峰值分别从 491／471MiB 降到 290／184MiB，详见调度说明。

## 公告准备

工作区 `_docs/公告.md` 已加入 v4.6.2 中英文正文，并保留 v4.6.0 内容；该文件不进入仓库提交，公告也尚未在网站发布。网站公告发布与线上版本切换属于合入 main 之后的发布步骤。

## 生产环境只读核对（2026-09-24）

- 生产数据库不存在 `statistics_jobs` / `statistics_snapshots`，主机也没有统计 Worker 服务或定时任务：统计迁移与 Worker 均未执行。
- 现站运行时版本仍为 `v4.6.0`，与本候选尚未切换一致。
- 主机容量：主库主机约 6GB 内存、可用 1665MiB，swap 已用 3272/4095；另有一台可达主机 2GB、可用 418MiB，第三台连接超时。优化已降低本地任务内存，仍须结合实际数据库负载重新确认共机运行余量。
- 结论：启用 Worker 前须先确认满足内存与 swap 余量的常驻主机，再按 [调度说明](STATISTICS_SCHEDULING.md) 执行迁移与预热。

## 发布前仍需完成

- [x] 版本、公告草稿、文档与提交范围核对，最终全量测试、lint、构建、统计 SQL、baseline 与生产依赖审计通过。
- [ ] 维护者审查：Git 工作流程要求合入至少一名审查者；仓库目前只有 `MoguJunn` 一个管理员账号，作者无法批准自己的 PR，需要其他维护者审查或在流程中记录例外。这是当前合入 main 的实际阻塞。
- [ ] 从 `feat/banner-stats-and-guide` 创建发布 PR 并通过 CI；随后保持未合并，等待新卡池适配完成和用户后续指令。
- [ ] 核对生产已执行迁移清单，再按调度说明应用统计迁移；本地文件和 baseline 尾号不能代替生产执行证据。
- [ ] 在容量达标的主机部署并启用常驻 Worker，核验任务租约、修订冲突、失败退避、队列延迟及最大范围耗时。
- [ ] 确认公开单池、五类合池、旧统计及个人范围均已完成 v4 快照预热，再启用前端／API。
- [ ] 发布网站公告，并核对运行时版本、生产构建、实际页面与认证后的个人读取，补充部署时间和真实结果。

注意：主站生产由 GitHub `main` 推送自动部署，合入 main 等同于部署前端。因此统计迁移、常驻 Worker 与 v4 快照预热须先完成，或已明确新版统计入口在合入时保持未启用，才可执行合并。

记录缺失可能使本期保存记录内的首次成本偏低；完整游戏前缀、武器批次顺序与理论规则原始来源仍有证据边界。指南真实业务接入、全站设计 token 与完整动画生命周期继续由后续任务处理。
