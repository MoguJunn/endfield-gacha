# ============================================
# 终末地抽卡分析器 - 环境变量配置示例
# ============================================
# 使用方法:
# 1. 复制此文件并重命名为 .env / .env.local
# 2. 填写下面的实际配置值
# 3. 重启开发服务器使配置生效
# 4. 若本地 dev middleware 需要读取服务端密钥，也可放在 backend/.env.local

# ====== Supabase 配置 (必需) ======
# 获取方式: Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx    # server/dev middleware alias
# PUBLISHABLE_KEY=sb_publishable_xxx             # Supabase Dashboard short alias
# VITE_SUPABASE_ANON_KEY=sb_publishable_xxx       # legacy alias
# SUPABASE_ANON_KEY=sb_publishable_xxx            # legacy server/dev alias
SUPABASE_SECRET_KEY=sb_secret_xxx
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx          # legacy alias

# 仅配置 SUPABASE_URL/PUBLISHABLE_KEY 或 VITE_SUPABASE_* 时，浏览器端与部分
# 用户级同源 API 会通过当前用户 Bearer token + RLS 工作；后台、邮件、OAuth
# 桥接、站点 session、运营自动化等高权限服务端功能仍必须配置 SUPABASE_SECRET_KEY
# 或 SUPABASE_SERVICE_ROLE_KEY。本地经 nginx 反代时也需要把 VITE_APP_URL / allowed
# origin 设置为实际访问域名，并重启 npm run dev。

# ====== 应用配置 (必需) ======
# 应用的公开访问域名，用于:
# 1. Auth 回跳 / 浏览器端链接生成
# 2. "急"按钮实时同步功能
# 3. 本地 dev middleware / Serverless allow-origin
VITE_APP_URL=https://your-domain.vercel.app

# 仅供 Vite 本地开发的贡献者内容沙盒。生产构建会强制忽略此开关。
VITE_CONTRIBUTOR_DEMO_MODE=false
# 贡献者沙盒启用时读取真实游戏目录的公共 API 基址；仅允许可信只读接口。
VITE_CONTRIBUTOR_CATALOG_API_BASE=https://ef-gacha.mogujun.icu
# 自建只读目录镜像和额外资源 CDN 必须显式加入主机白名单（逗号分隔，不含协议和路径）。
VITE_CONTRIBUTOR_CATALOG_ALLOWED_HOSTS=
VITE_PUBLIC_RESOURCE_ALLOWED_HOSTS=

# 同域活动子站。浏览器端用于首页入口，服务端保留 /lottery 作为 SSO 回调基础路径。
VITE_SUMMER_LOTTERY_URL=/lottery
LOTTERY_SITE_URL=https://your-domain.vercel.app/lottery
MAIN_SITE_URL=https://your-domain.vercel.app
LOTTERY_CAMPAIGN_ID=arknights-p3r-collab-2026
LOTTERY_SSO_AUDIENCE=open-lottery
# 用户数据面专用网关密钥；至少 32 随机字节，不得复用会话或开奖密钥。
LOTTERY_BACKEND_SECRET=replace-with-43-char-base64url-random-secret
# 活动子站会话密钥；不得复用主站 APP_SESSION_SECRET。
LOTTERY_SESSION_SECRET=replace-with-43-char-base64url-random-secret
LOTTERY_SESSION_TTL_SECONDS=604800
# 开奖私有种子只配置在主站服务端。开放参与前固定；开奖后由公开快照披露供复算。
LOTTERY_DRAW_SEED=replace-with-43-char-base64url-random-seed
# 联系方式在进入数据库前使用 AES-256-GCM 加密。keyring 值是“key id -> 32 字节 base64url 密钥”的 JSON。
# 轮换时先加入新 key 并切换 ACTIVE_KEY_ID；历史数据仍需解密期间保留旧 key。
LOTTERY_CONTACT_ENCRYPTION_ACTIVE_KEY_ID=contact-2026-01
LOTTERY_CONTACT_ENCRYPTION_KEYS_JSON={"contact-2026-01":"replace-with-43-char-base64url-32-byte-key"}
LOTTERY_TIME_ZONE=Asia/Shanghai
LOTTERY_BODY_FONT_STYLESHEET_URL=/lottery/local-activity/fonts/site-fonts.css
LOTTERY_BRAND_MARK=EG
LOTTERY_BRAND_NAME=Endfield Gacha
LOTTERY_BRAND_TAGLINE=COLLABORATION EVENT 2026
LOTTERY_FOOTER_NOTICE=本活动由终末地抽卡分析器社区发起，感谢 Neptune（备用网址提供者）赞助本期奖品；具体领取方式以中奖通知为准。
LOTTERY_HERO_KICKER=ARKNIGHTS × PERSONA 3 RELOAD
LOTTERY_HERO_TITLE_LINE_ONE=Arknights
LOTTERY_HERO_TITLE_LINE_TWO=Persona 3 Reload
LOTTERY_HERO_YEAR_BADGE=26
LOTTERY_HERO_TITLE=明日方舟 × P3R 联动特别抽奖
LOTTERY_HERO_DESCRIPTION=登录活动账号领取参与编号，一等奖可在明日方舟与终末地月卡中任选一张。
LOTTERY_SPONSOR_CREDIT=感谢 Neptune（备用网址提供者）赞助本期奖品
LOTTERY_ARCHIVE_URL=/lottery/archive/community-lottery
LOTTERY_ARCHIVE_LABEL=查看上一期开奖结果
LOTTERY_ENTRY_PREFIX=P3R26
LOTTERY_PRIZES_JSON=[{"tier":"first","rank":"01","title":"明日方舟 / 终末地月卡二选一","description":"共 1 名，中奖者可任选一张明日方舟月卡或终末地月卡。","icon":"gift","imageUrls":["/lottery/local-activity/arknights-monthly-card.png","/lottery/local-activity/endfield-monthly-card.png"]},{"tier":"second","rank":"02","title":"方舟本体 × P3R 联动 6 元礼包","description":"共 5 名，每人获得一份联动 6 元礼包；不可折现。","icon":"sparkles","imageUrl":""},{"tier":"participation","rank":"ALL","title":"后续小游戏站内测资格","description":"所有成功参与且未获得一、二等奖的用户，可获得后续小游戏站上线后的内测资格。","icon":"ticket","imageUrl":""}]
LOTTERY_RULES_JSON={"version":"明日方舟 × P3R 联动特别抽奖正式规则","notice":"本期为独立活动；上一期参与编号、候选名单与开奖结果继续保留在往期公示中，但不计入本期。提交参与即表示已阅读并同意以下规则。","items":[{"title":"参与资格与次数","description":"使用主站账号登录抽奖站即可参与。每个主站账号在本期只能取得一个有效参与编号；重复登录、重复提交或更换联系方式均不会增加中奖机会。"},{"title":"有效报名认定","description":"须在截止前完成领奖 QQ 号填写、机器人验证并收到页面的“已参与”确认；仅打开页面、登录但未提交，或提交失败，均不视为有效报名。最终资格以服务端保存的本期参与记录为准。"},{"title":"活动时间与开奖安排","description":"北京时间 2026 年 8 月 21 日 20:30 开放，2026 年 9 月 15 日 12:00 截止，截止后不再补录。系统最早允许在 12:00:03 开奖；实际开奖由活动方在截止后人工执行，可能晚于该时间。"},{"title":"奖品、名额与限制","description":"一等奖 1 名，中奖者可在明日方舟月卡与终末地月卡中任选一张；二等奖 5 名，每人一份明日方舟本体 × P3R 联动 6 元礼包。一、二等奖均不可折现或转让，同一参与编号最多获得其中一项。"},{"title":"候选名单冻结与隐私","description":"截止后将冻结全部有效参与编号并生成候选清单哈希。公开核对数据只包含随机公开 ID、参与编号及必要审计字段，不公开 QQ、账号身份、会话信息或其他私密资料。"},{"title":"公开公平开奖","description":"活动开始前先锁定私有种子的 SHA-256 承诺；开奖时结合该种子与 drand Quicknet 第 32213813 轮公共随机数，从已冻结的候选清单中确定中奖者，开奖方无法在看到公共随机数后替换种子或候选名单。"},{"title":"开奖结果与独立复查","description":"开奖后公开中奖编号、脱敏昵称、种子原文、drand 轮次与随机数、候选清单哈希及复算状态。任何人均可使用同一公开数据独立复算；若核对异常，将暂停发奖并公开说明。"},{"title":"中奖联系、领取与补抽","description":"活动方通过报名时加密保存的 QQ 联系中奖者。中奖者须在首次通知后 7 日内回复；一等奖同时确认月卡选择。无法联系、逾期未回复、主动放弃或资格被取消时，可按相同公开流程补抽，并公示原因与结果。"},{"title":"参与奖发放","description":"所有有效参与且未获得一、二等奖的用户，可获得后续小游戏站内测资格。待测试条件具备后统一通知；资格不可转让、不可折现，测试范围、周期、名额管理和保密要求以上线通知为准。"},{"title":"赞助与活动独立性","description":"感谢 Neptune（备用网址提供者）赞助本期奖品。赞助方不接触报名联系方式，不参与候选名单冻结、随机数选取、中奖判定或异常资格审核。"},{"title":"违规及异常处理","description":"禁止利用漏洞、脚本、批量账号、虚假身份或其他不正当手段影响活动。发现异常可取消相关资格；如遇服务故障、drand 延迟或不可抗力，将保留审计记录并公开处理方案，必要调整不得追溯性改变已冻结候选名单。"}]}
LOTTERY_CONTACT_FORMAT=qq
LOTTERY_CONTACT_LABEL=领奖 QQ 号
LOTTERY_CONTACT_PLACEHOLDER=请输入你的 QQ 号
LOTTERY_CONTACT_PRIVACY_NOTICE=QQ 号会加密保存，只用于中奖后联系发奖，不会公开展示或用于其他用途。
LOTTERY_NOTIFICATION_REQUIRED=false
LOTTERY_NOTIFICATION_LABEL=活动通知渠道
LOTTERY_NOTIFICATION_VALUE=
LOTTERY_NOTIFICATION_DESCRIPTION=活动结果公布后会通过配置的通知渠道提醒参与者。
LOTTERY_NOTIFICATION_CONFIRMATION_LABEL=我已加入活动通知渠道

# 生产环境公共数据默认只走同源 /api。
# 若本地开发或应急回退仍需要浏览器直连 Supabase，可显式打开：
VITE_PUBLIC_DATA_DIRECT_SUPABASE_FALLBACK=false

# 浏览器端 Realtime 默认关闭，避免本地/无代理网络出现 Supabase WebSocket 超时。
# 仅在需要调试角色/卡池实时订阅，并确认网络能直连 Supabase Realtime 时开启。
VITE_SUPABASE_REALTIME_ENABLED=false

# 邮件防刷 HMAC 盐，仅服务端使用。不要暴露给前端。
# 未配置时本地开发会回退到 SUPABASE_JWT_SECRET 或本地默认盐；生产环境应显式设置。
MAIL_ABUSE_HASH_SECRET=replace-me-with-long-random-secret

# 自建邮件平台配置。默认按 Stalwart 优先规划。
# 当前已有 outbox 队列处理器 / provider adapter 边界，但默认使用演练模式，不发送真实邮件。
# 不要在公开仓库提交真实 SMTP/JMAP 凭据、DKIM 私钥或服务器 IP。
MAIL_PROVIDER=stalwart
MAIL_FROM_ADDRESS=no-reply@example.com
MAIL_FROM_NAME=Endfield Gacha
# 注册确认、密码重置、邮件登录的同源 Auth 邮件入口。
# 真实发信前必须同时完成 CAPTCHA / 限流 / 紧急停发开关 / Stalwart SMTP 配置。
AUTH_MAIL_ACTIONS_ENABLED=false
ACCOUNT_RECOVERY_MAIL_OUTBOX_ENABLED=false
# 开发者 API 审核结果通知。需要同时启用 MAIL_OUTBOX_WORKER_ENABLED；默认关闭。
DEVELOPER_API_REVIEW_MAIL_OUTBOX_ENABLED=false
# 工单 staff 回复邮件通知。需要同时启用 MAIL_OUTBOX_WORKER_ENABLED；默认关闭。
TICKET_REPLY_MAIL_OUTBOX_ENABLED=false
# 管理员受控自告警邮件。只允许发送给当前 super_admin 自己；默认关闭。
ADMIN_ALERT_MAIL_OUTBOX_ENABLED=false
MAIL_OUTBOX_WORKER_ENABLED=false
MAIL_WORKER_DRY_RUN=true
MAIL_WORKER_BATCH_SIZE=10
MAIL_WORKER_MAX_ATTEMPTS=3
MAIL_WORKER_RETRY_DELAY_SECONDS=900
MAIL_PROVIDER_TIMEOUT_MS=15000
MAIL_OUTBOX_GLOBAL_KILL_SWITCH=true
# 内部队列处理 endpoint secret。/api/mail-outbox-worker 同时接受此值和 CRON_SECRET，
# 方便 Vercel Cron 使用 Authorization: Bearer <CRON_SECRET> 触发；生产仍建议单独设置此值给外部 cron / 运维脚本。
MAIL_OUTBOX_WORKER_SECRET=

# 私有个人分析快照 Worker。生产调度由自建 Supabase `pg_cron + pg_net`
# 每分钟兜底，活跃用户排队后可即时派发；GitHub Actions 只保留手动应急入口。
# 不使用 Vercel 高频 Cron（Hobby 套餐只允许每日 Cron）。
PERSONAL_ANALYSIS_WORKER_ENABLED=false
PERSONAL_ANALYSIS_WORKER_SECRET=
# 常规任务保持 false；仅人工执行一次性历史回填时临时开启。
PERSONAL_ANALYSIS_WORKER_BACKFILL_ENABLED=false
# 每批默认只领取一个用户作业；受保护 HTTP 调用可在 45 秒预算内顺序处理多批。
PERSONAL_ANALYSIS_WORKER_BATCH_SIZE=1
# 仅用于本地无持久快照时的同步回退；生产环境应保持关闭。
PERSONAL_ANALYSIS_TRANSIENT_FALLBACK=false
STALWART_SMTP_HOST=
STALWART_SMTP_PORT=587
STALWART_SMTP_USERNAME=
STALWART_SMTP_PASSWORD=
STALWART_SMTP_SECURE=false
STALWART_SMTP_STARTTLS=true
STALWART_SMTP_TLS_REJECT_UNAUTHORIZED=true
STALWART_SMTP_ALLOW_INSECURE=false
STALWART_JMAP_URL=
STALWART_WEBHOOK_SECRET=
# 内部投递反馈 / 退信回写 secret。可与 STALWART_WEBHOOK_SECRET 分开；不要暴露给前端。
MAIL_DELIVERY_WEBHOOK_SECRET=
# 内部入站邮件事件 secret。用于 Stalwart Webhooks / MTA Hooks 调用 /api/mail-inbound。
MAIL_INBOUND_WEBHOOK_SECRET=
# Postal 是后续独立邮件 VPS / 事务邮件平台化的备选，不是当前同机部署默认。
POSTAL_API_URL=
POSTAL_API_KEY=
POSTAL_SMTP_HOST=
POSTAL_SMTP_PORT=587
POSTAL_SMTP_USERNAME=
POSTAL_SMTP_PASSWORD=
POSTAL_WEBHOOK_SECRET=
# SMTP EHLO / Message-ID domain. Prefer the mail host with matching A/PTR, e.g. mail.example.com.
MAIL_SENDING_DOMAIN=mail.example.com

# 认证安全审计 / 风险识别 HMAC 盐，仅服务端使用。生产环境建议显式设置。
# 未配置时会回退到 MAIL_ABUSE_HASH_SECRET / SUPABASE_JWT_SECRET。
AUTH_SECURITY_HASH_SECRET=replace-me-with-long-random-secret

# 登录 / 注册 / 恢复申请的服务端 CAPTCHA 策略。
# off: 不校验；monitor: 记录风险但不拦截；enforce: 对 AUTH_CAPTCHA_REQUIRED_ACTIONS 强制校验。
# provider 支持 turnstile / hcaptcha；secret 只放服务端环境变量。
AUTH_CAPTCHA_MODE=off
AUTH_CAPTCHA_PROVIDER=turnstile
AUTH_CAPTCHA_REQUIRED_ACTIONS=register,password_reset,account_recovery,lottery_enter
AUTH_CAPTCHA_SECRET_KEY=

# 浏览器端只允许放 CAPTCHA site key；不要放 secret。
# 与服务端策略保持同一 action 列表。生产开启 enforce 前需要真实 provider token 能透传到 /api/auth-rate-limit。
VITE_AUTH_CAPTCHA_MODE=off
VITE_AUTH_CAPTCHA_PROVIDER=turnstile
VITE_AUTH_CAPTCHA_REQUIRED_ACTIONS=register,password_reset,account_recovery,lottery_enter
VITE_AUTH_CAPTCHA_SITE_KEY=
VITE_TURNSTILE_SITE_KEY=
VITE_HCAPTCHA_SITE_KEY=

# 第三方一键登录。GitHub / Linux.do / QQ 默认走本站同源 OAuth 桥接；
# GoTrue 内置 / Custom Provider 仅保留为诊断或回退参考。
# 所有 CLIENT_SECRET / App Key 只放服务端或 Supabase Auth 容器环境变量；
# 浏览器端只用 VITE_AUTH_OAUTH_*_ENABLED 控制按钮显示。
#
# OAUTH_STATE_SECRET 只签名短期 OAuth state / 浏览器绑定，不得再用于持久 identity hash。
# AUTH_IDENTITY_HASH_KEY_CURRENT 是独立的 provider subject HMAC 密钥；版本号随轮换递增。
# 轮换期间把上一把密钥和版本放入 PREVIOUS；确认旧 hash 已全部迁移后才可移除。
# LEGACY_STATE 只在迁移 legacy_state_v1 identity 时读取。若历史部署轮换过 state 密钥，
# 必须填入历史 identity 实际使用值；全部 legacy 行迁移完后才可移除。
# APP_SESSION_SECRET 只用于本站 HttpOnly session、请求指纹和兼容 token 相关摘要。
# SUPABASE_JWT_SECRET 只在过渡期用于签发短期 Supabase 兼容 token，帮助现有 RLS 直连平滑迁移。
OAUTH_STATE_SECRET=replace-me-with-long-random-secret
AUTH_IDENTITY_HASH_KEY_LEGACY_STATE=replace-me-with-the-historical-identity-secret
AUTH_IDENTITY_HASH_KEY_CURRENT=replace-me-with-independent-identity-hash-key
AUTH_IDENTITY_HASH_KEY_CURRENT_VERSION=v2
AUTH_IDENTITY_HASH_KEY_PREVIOUS=
AUTH_IDENTITY_HASH_KEY_PREVIOUS_VERSION=
APP_SESSION_SECRET=replace-me-with-another-long-random-secret
APP_SESSION_COOKIE_NAME=__Host-eg_session
# 刷新凭据 Cookie 仅发送到 session 端点，因此使用允许受限 Path 的 __Secure- 前缀。
APP_REFRESH_COOKIE_NAME=__Secure-eg_refresh
APP_SESSION_TTL_SECONDS=7200
APP_SESSION_IDLE_TTL_SECONDS=604800
APP_SESSION_ABSOLUTE_TTL_SECONDS=2592000
APP_SESSION_COMPAT_JWT_ENABLED=true
APP_SESSION_COMPAT_JWT_TTL_SECONDS=3600

# 服务端 OAuth 出站请求默认不读取系统代理。仅当部署/开发网络必须通过
# HTTPS_PROXY / HTTP_PROXY 访问 provider 时开启；NO_PROXY 仍会生效。
AUTH_OAUTH_USE_ENV_PROXY=false
# HTTPS_PROXY=http://proxy.example.com:7890
# HTTP_PROXY=http://proxy.example.com:7890
# NO_PROXY=127.0.0.1,localhost

AUTH_OAUTH_LINUXDO_ENABLED=false
AUTH_OAUTH_LINUXDO_CLIENT_ID=
AUTH_OAUTH_LINUXDO_CLIENT_SECRET=
AUTH_OAUTH_LINUXDO_REDIRECT_URI=https://your-domain.vercel.app/api/auth/oauth/linuxdo/callback
AUTH_OAUTH_LINUXDO_SCOPE=openid profile email
AUTH_OAUTH_LINUXDO_TOKEN_AUTH_METHOD=basic
# 默认不向 Linux.do 显式发送 redirect_uri，使用 Linux.do 后台登记的回调地址。
# 如果后续确认平台要求请求中必须带回调地址，再改为 true。
AUTH_OAUTH_LINUXDO_SEND_REDIRECT_URI=false
VITE_AUTH_OAUTH_LINUXDO_ENABLED=false

AUTH_OAUTH_GITHUB_ENABLED=false
AUTH_OAUTH_GITHUB_CLIENT_ID=
AUTH_OAUTH_GITHUB_CLIENT_SECRET=
AUTH_OAUTH_GITHUB_REDIRECT_URI=https://your-domain.vercel.app/api/auth/oauth/github/callback
AUTH_OAUTH_GITHUB_SCOPE=read:user user:email
# 默认向 GitHub 显式发送本站桥接回调地址，避免 GitHub 回到 Supabase Auth 后产生 bad_oauth_state。
AUTH_OAUTH_GITHUB_SEND_REDIRECT_URI=true
VITE_AUTH_OAUTH_GITHUB_ENABLED=false

AUTH_OAUTH_QQ_ENABLED=false
AUTH_OAUTH_QQ_CLIENT_ID=
AUTH_OAUTH_QQ_CLIENT_SECRET=
AUTH_OAUTH_QQ_REDIRECT_URI=https://your-domain.vercel.app/api/auth/oauth/qq/callback
AUTH_OAUTH_QQ_SCOPE=get_user_info
VITE_AUTH_OAUTH_QQ_ENABLED=false

# 可选认证风险域名规则，逗号分隔。只影响同源 API 的服务端风险桶 / 审计。
AUTH_RISK_HIGH_EMAIL_DOMAINS=
AUTH_RISK_BLOCKED_EMAIL_DOMAINS=

# 账号恢复人工降级链：管理员发放临时密码后的有效期，单位小时。
# 默认 24，服务端最多接受 168。
ACCOUNT_RECOVERY_TEMP_PASSWORD_TTL_HOURS=24

# 发布时如需强制所有访客刷新一次公共快照，可 bump 这个 token
VITE_APP_FORCE_REFRESH_TOKEN=2026-05-22-release-refresh

# ====== 可选配置 ======

# 外部代理服务器地址（可选）
# 如果不配置，将走站内 Serverless / 本地 dev middleware；完整导入能力仍取决于你的私有代理是否可用
#
# 单后端模式示例: https://endfield-proxy.railway.app
# VITE_PROXY_URL=https://your-backend-server.railway.app
#
# 双后端模式示例（推荐用于国服 / 国际服分离部署）
# VITE_PROXY_URL_CN=https://your-cn-backend.railway.app
# VITE_PROXY_URL_INTL=https://your-intl-backend.railway.app
VITE_PROXY_URL_CN=https://your-cn-proxy.example.com
VITE_PROXY_URL_INTL=https://your-intl-proxy.example.com

# 验证码玩家站（可选）
# 若未配置，将回退到站内默认路径或隐藏对应跳转
VITE_PUZZLE_PLAYER_URL=https://your-puzzle-player.example.com

# ====== 公共自动化与 Cron（可选） ======

# Vercel Cron 鉴权 secret。除现有自动化外，每日联系方式到期清理任务也使用该值；
# 清理任务只删除抽奖联系方式密文，保留参与编号、公开结果与中奖记录。
CRON_SECRET=replace-me

# OPS-006：官方公告 / 卡池 / 图鉴巡检源
OPS_AUTOMATION_ANNOUNCEMENTS_URL=https://example.com/announcements.json
OPS_AUTOMATION_ANNOUNCEMENTS_TAG=official-json
OPS_AUTOMATION_POOL_SCHEDULE_URL=https://example.com/pools.json
OPS_AUTOMATION_POOL_SCHEDULE_TAG=official-json
OPS_AUTOMATION_WIKI_CATALOG_URL=https://example.com/wiki.json
OPS_AUTOMATION_WIKI_CATALOG_TAG=official-json

# ====== 官方 BOT（可选） ======

# QQ Bot 卡池更新推送代理。主站只暴露给超级管理员，浏览器不会接触 QQ Bot 密钥。
# QQBOT_POOL_PUSH_WEBHOOK_SECRET 必须与 Bot 侧 QQBOT_POOL_UPDATE_ADMIN_SECRET 一致。
QQBOT_POOL_PUSH_ENABLED=false
QQBOT_POOL_PUSH_WEBHOOK_URL=http://127.0.0.1:8787
QQBOT_POOL_PUSH_WEBHOOK_SECRET=
QQBOT_POOL_PUSH_TIMEOUT_MS=10000

# 官方 Telegram BOT
TELEGRAM_OFFICIAL_BOT_PUBLIC_API_KEY=replace-me
TELEGRAM_OFFICIAL_BOT_VERIFIER_SECRET=replace-me
TELEGRAM_OFFICIAL_BOT_TOKEN=replace-me
TELEGRAM_OFFICIAL_BOT_PROXY_URL=http://127.0.0.1:7890
TELEGRAM_OFFICIAL_BOT_POLL_INTERVAL_MS=1500
TELEGRAM_OFFICIAL_BOT_LONG_POLL_SECONDS=20

# ====== 后端可选变量（部署私有 backend/ 时使用） ======

# TG_BOT_TOKEN=your-telegram-bot-token
# TG_CHAT_ID=your-default-chat-id
# TG_API_BASE=https://api.telegram.org
#
# 双后端推荐分别设置独立群/频道，避免国服和国际服混发
# TG_CHAT_ID_CN=-100xxxxxxxxxx
# TG_CHAT_ID_INTL=-100yyyyyyyyyy
# TG_CHAT_ID_ALL=-100zzzzzzzzzz
#
# Telegram 告警节流窗口（毫秒）
# TG_ALERT_THROTTLE_MS=180000
#
# 后端日志时区，影响服务端 console 时间戳
# LOG_TIMEZONE=Asia/Shanghai

# ============================================
# 📌 重要提示
# ============================================
#
# 1. Supabase 配置:
#    - 确保已创建所有必需的数据库表和迁移
#    - 当前新环境优先直接执行 supabase/baseline/000_complete_schema.sql
#    - 当前生成 baseline 覆盖到 active/183_split_reconstruction_claim_subtype.sql；不要重复叠加已包含迁移
#    - 已确认生产历史包括独立抽奖 160–165、认证 166–168 和正式导入修复 170；后续运行态仍需实时核验
#    - 若要在本地调试账号恢复 / bootstrap / 受限管理接口，还必须额外配置 SUPABASE_SECRET_KEY
#
# 2. Realtime 功能:
#    - 浏览器端默认不建立 Realtime WebSocket；公共首屏数据通过同源 /api 和快照刷新。
#    - 需要调试角色 / 卡池实时订阅时，设置 VITE_SUPABASE_REALTIME_ENABLED=true。
#    - 若开启，还需在 Supabase Dashboard -> Database -> Replication 中启用对应表。
#
# 3. 账号恢复 / 自建邮件:
#    - 注册确认、密码重置和邮件登录走同源 /api/auth-email-action，默认 AUTH_MAIL_ACTIONS_ENABLED=false
#    - 开启认证邮件前必须配置 Stalwart SMTP、AUTH_CAPTCHA_MODE / site key、紧急停发开关和投递监控
#    - 账号恢复优先走自助密码重置邮件；多次收不到邮件或邮箱不可用时才提交人工恢复申请
#    - 只有 ACCOUNT_RECOVERY_MAIL_OUTBOX_ENABLED=true 且 MAIL_OUTBOX_WORKER_ENABLED=true 时，人工恢复申请中的 password_reset 才会写入 mail_outbox
#    - 开发者 API 审核通知由 DEVELOPER_API_REVIEW_MAIL_OUTBOX_ENABLED 控制；入队失败不阻断审核
#    - 工单 staff 回复通知由 TICKET_REPLY_MAIL_OUTBOX_ENABLED 控制；管理员自告警由 ADMIN_ALERT_MAIL_OUTBOX_ENABLED 控制
#    - Phase C 已通过 Auth session 门禁和站点 Session/Bearer 检查执行临时凭据到期，并已随认证主线发布
#    - auth_security_events 只保存 hash、风险桶和脱敏摘要，不保存原始邮箱 / 密码 / CAPTCHA token / game_uid
#    - AUTH_CAPTCHA_MODE=enforce 前必须先接入真实 Turnstile / hCaptcha 前端 token，否则注册 / 恢复会被服务端拦截
#    - 自建邮件主线详见 docs/SELF_HOSTED_MAIL.md；当前只有认证邮件入口允许受控同步发信，不提供通用公开发送邮件 API
#    - 邮件入队使用 MAIL_ABUSE_HASH_SECRET 生成收件人 hash、预算桶 key 和幂等 key
#    - MAIL_WORKER_DRY_RUN=true 表示演练模式：只验证队列处理 / 模板 / 状态回写，不代表真实投递
#    - MAIL_WORKER_DRY_RUN=false 前必须完成真实传输、DNS、suppression、紧急停发开关和投递监控
#    - 若要让国服 / 国际服后端分别推送到不同 Telegram 群，请同时配置 TG_CHAT_ID_CN / TG_CHAT_ID_INTL
#
# 4. Edge Functions:
#    - 旧 admin-create-user / admin-delete-user 仅保留兼容说明
#    - 当前后台主链已收口到 Vercel Serverless `/api/admin`
#
# ============================================
